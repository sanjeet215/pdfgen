# Dev profile overrides
# Leave empty to allow any origin without credentials (handled by CorsConfig)
# Switch profile with: -Dspring.profiles.active=dev
cors.allowed-origins=
cors.allow-credentials=false
